JSS Portfolio - unified CSS version

Upload all files and the assets folder to your hosting file manager.

Important updates in this version:
- All pages use one shared stylesheet: style.css
- Page-level <style> blocks were removed
- Inline CSS was removed from the HTML pages
- The loader, navbar, buttons, cards, animations, forms, and responsive layout are consistent across all pages
- script.js controls the loader, mobile menu, active navigation, scroll reveal animation, and contact form mailto action

After replacing the old files, refresh with Ctrl + F5 or clear your browser cache if the old design still appears.
